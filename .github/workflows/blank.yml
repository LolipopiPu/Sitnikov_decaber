on:
  push:
    branches: [ "main" ]
  workflow_dispatch:
  
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3 
      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - run: docker build -t ghcr.io/LolipopiPu/Sitnikov_decaber:1.0 .
      - run: docker push ghcr.io/LolipopiPu/Sitnikov_decaber:1.0
      - run: ssh -o "StrictHostKeyChecking no" 'lolipopipu-sitnikovdeca-lepq43sirsv#c8eBn4I-LwM1vWTrJ.Q.8Pw2aoOdxCxF@lolipopipu-sitnikovdeca-lepq43sirsv.ssh.ws-eu106.gitpod.io' 'cd /workspace/Sitnikov_decaber/ && docker compose pull && docker compose up -d'
      - name: send default message
        if: success()
        uses: appleboy/telegram-action@master
        with:
          message: "Перемога будэ!"
          to: 851544476
          token: 6482782409:AAFTHDJiprOlCPVjecmW4kCUIpiLsQl__Mo
      - name: send default message
        if: failure()
        uses: appleboy/telegram-action@master
        with:
          message: "unlucky"
          to: 851544476
          token: 6482782409:AAFTHDJiprOlCPVjecmW4kCUIpiLsQl__Mo
